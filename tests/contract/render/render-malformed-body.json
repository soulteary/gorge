{
  "name": "malformed json body is rejected",
  "description": "A truncated or otherwise invalid request body yields 400 with the shared ERR_BAD_REQUEST code rather than a 500.",
  "request": {
    "method": "POST",
    "path": "/api/highlight/render",
    "headers": {
      "Content-Type": "application/json",
      "X-Service-Token": "contract-token"
    },
    "body": "{\"source\":"
  },
  "expect": {
    "status": 400,
    "jsonEquals": { "error.code": "ERR_BAD_REQUEST" },
    "jsonAbsent": ["data"]
  }
}
