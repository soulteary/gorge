{
  "name": "?token= is accepted alongside the header",
  "description": "The query parameter is the fallback for callers that cannot set headers, and it is checked only when X-Service-Token is absent. The PHP client uses the header; this path exists for the curl one-liners in a runbook, and the read endpoint is where they are most useful — pulling one file out of storage to look at it.",
  "request": {
    "method": "GET",
    "path": "/api/file/engines?token=contract-token"
  },
  "expect": {
    "status": 200,
    "jsonHas": ["data.0.identifier"],
    "jsonAbsent": ["error"]
  }
}
