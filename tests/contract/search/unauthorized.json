{
  "name": "missing service token is rejected",
  "description": "When the service is configured with a token, a request without one gets 401 and an error envelope. The response must carry no data field, so a caller that ignores the status code still cannot mistake it for an indexed document.",
  "request": {
    "method": "POST",
    "path": "/api/search/index",
    "headers": {
      "Content-Type": "application/json"
    },
    "body": "{\"phid\":\"PHID-TASK-notoken\",\"type\":\"TASK\",\"title\":\"no token\"}"
  },
  "expect": {
    "status": 401,
    "jsonHas": ["error.code", "error.message"],
    "jsonEquals": { "error.code": "ERR_UNAUTHORIZED" },
    "jsonAbsent": ["data"]
  }
}
